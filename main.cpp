// #include "Simulator.h"
#include "UI.h"

// TODO: make the README

int main() {
    UI ui;
    ui.menu();

    /* //DEBUG MODE
    Simulator s;
    s.initReserve(21, 21);
    s.startSimulation();
    s.addEntity(1, 1, EntityTypes::animal, 'c');
    s.getReserve().getGrid(); */

    return 0;
}

#include "Simulator.h"
#include "UI.h"

int main() {
    UI ui;
    ui.menu();

    /* Simulator s;
    s.initReserve(21, 21);
    s.startSimulation();
    s.addEntity(1, 1, EntityTypes::animal, 'c');
    s.getReserve().getGrid(); */

    return 0;
}
